---
import { SITE_URL_FULL } from "@/data/config";
import type { HeadTags } from "@/utils/types/HeadTags";
import { AstroFont } from "astro-font";
import { SEO } from "astro-seo";

type Props = HeadTags;

const { title, description, noindex, og } = Astro.props;

const DEFAULT_TITLE_PAGE = "Portfolio | Kyle Carhart";
const DEFAULT_DESCRIPTION_PAGE =
  "Portfolio of Kyle Carhart, a Senior Full Stack Developer from Fayetteville, NC.";
const DEFAULT_URL_SITE = SITE_URL_FULL;

const openGraph = {
  title: title || og?.title || DEFAULT_TITLE_PAGE,
  type: og?.type || "website",
  image: og?.image || "/opengraph-image.jpg",
  alt: og?.alt || "astro portfolio template image",
  url: DEFAULT_URL_SITE,
  description: og?.description || DEFAULT_DESCRIPTION_PAGE,
};
---

<head>
  <SEO
    charset="UTF-8"
    title={title || DEFAULT_TITLE_PAGE}
    description={description || DEFAULT_DESCRIPTION_PAGE}
    noindex={noindex || false}
    openGraph={{
      basic: {
        title: openGraph.title,
        type: openGraph.type,
        image: openGraph.image,
      },
      image: {
        alt: openGraph.alt,
      },
    }}
    twitter={{
      creator: "@itsstormzz_",
    }}
    extend={{
      link: [
        { rel: "icon", href: "/favicon.svg" },
        { rel: "sitemap", href: "/sitemap-index.xml" },
      ],
      meta: [
        { name: "viewport", content: "width=device-width, initial-scale=1" },
        { name: "generator", content: Astro.generator },
        {
          name: "twitter:image",
          content: openGraph.image,
        },
        { name: "twitter:card", content: "summary_large_image" },
        { name: "twitter:title", content: openGraph.title },
        { name: "twitter:description", content: openGraph.description },
        { name: "twitter:site", content: "@itsstormzz_" },
      ],
    }}
  />
  <AstroFont
    config={[
      {
        src: [],
        preload: false,
        display: "swap",
        selector: "html",
        name: "Open Sans",
        fallback: "sans-serif",
        cssVariable: "font-open-sans",
        googleFontsURL:
          "https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300..800;1,300..800&display=swap",
      },
      {
        src: [],
        preload: false,
        display: "swap",
        selector: "html",
        name: "Ubuntu Sans Mono",
        fallback: "sans-serif",
        cssVariable: "font-ubuntu-sans-mono",
        googleFontsURL:
          "https://fonts.googleapis.com/css2?family=Ubuntu+Sans+Mono:ital,wght@0,400..700;1,400..700&display=swap",
      },
    ]}
  />
</head>
